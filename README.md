# OlivieBot

Telegram bot for tracking food expiration dates built with Node.js, Telegraf, and Supabase. Deployed on Render.com with polling.

Set environment variables: `BOT_TOKEN`, `SUPABASE_URL`, `SUPABASE_KEY`, `PORT` (optional, default 4000).
